<UserControl x:Class="CoffeeShop.Wpf.Views.ReportNangCaoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <DataGrid ItemsSource="{Binding BaoCaoNangCaoRows}"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              CanUserAddRows="False">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Mã món" Binding="{Binding MonId}" Width="90" />
            <DataGridTextColumn Header="Tên sản phẩm" Binding="{Binding TenMon}" Width="2*" />
            <DataGridTextColumn Header="Số lượng bán" Binding="{Binding SoLuongBan}" Width="120" />
            <DataGridTextColumn Header="Doanh thu gộp" Binding="{Binding DoanhThuGop, StringFormat=N0}" Width="140" />
            <DataGridTextColumn Header="Giá bán TB" Binding="{Binding GiaBanTrungBinh, StringFormat=N0}" Width="130" />
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
