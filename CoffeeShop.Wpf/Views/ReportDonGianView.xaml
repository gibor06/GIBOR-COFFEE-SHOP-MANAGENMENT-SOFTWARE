<UserControl x:Class="CoffeeShop.Wpf.Views.ReportDonGianView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <DataGrid ItemsSource="{Binding BaoCaoDonGianRows}"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              CanUserAddRows="False"
              ScrollViewer.VerticalScrollBarVisibility="Auto"
              ScrollViewer.HorizontalScrollBarVisibility="Auto"
              >
        <DataGrid.Columns>
            <DataGridTextColumn Header="Ngày" Binding="{Binding Ngay, StringFormat=dd/MM/yyyy}" Width="130" />
            <DataGridTextColumn Header="Số hóa đơn" Binding="{Binding SoHoaDon}" Width="110" />
            <DataGridTextColumn Header="Tổng tiền" Binding="{Binding TongTien, StringFormat=N0}" Width="130" />
            <DataGridTextColumn Header="Giảm giá" Binding="{Binding TongGiamGia, StringFormat=N0}" Width="120" />
            <DataGridTextColumn Header="Doanh thu thuần" Binding="{Binding DoanhThuThuan, StringFormat=N0}" Width="150" />
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
